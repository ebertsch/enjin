angular.module('{{appName}}.{{pluralize moduleName}}', [
    '{{appName}}.{{pluralize moduleName}}.config',
    '{{appName}}.{{pluralize moduleName}}.resources',
    '{{appName}}.{{pluralize moduleName}}.resolvers',
    '{{appName}}.{{pluralize moduleName}}.filters',
    '{{appName}}.{{pluralize moduleName}}.directives',
    '{{appName}}.{{pluralize moduleName}}.services'
]);